// Traversing es recorrer por nodos del HTML, desde el padre, al hijo, etc
// Js se puede usar para elegir de un hijo a un padre

const navegacion = document.querySelector('#principal');

console.log(navegacion.children);//seleccionar los hijos
